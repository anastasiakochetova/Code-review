# 📊 Анализ датасета Wine — Exploratory Data Analysis (EDA)

Это проект для задания №2, посвящённый исследовательскому анализу данных (EDA) на основе классического датасета **Wine** из библиотеки `sklearn`.

---

## 🎯 Цель проекта
Провести полный анализ датасета Wine, включающий:

- загрузку и подготовку данных;
- анализ целевой переменной (классов вин);
- расчёт статистик по числовым признакам;
- визуализацию распределений;
- сравнение признаков по классам;
- построение матрицы корреляции признаков.

---

## 📁 Структура проекта
- `assignment.py` — основной файл с реализацией всех функций анализа.
- PNG-файлы с графиками:
  - `02_wine_target_distribution.png`
  - `02_wine_features_distribution.png`
  - `02_wine_features_by_class.png`
  - `02_wine_correlation_matrix.png`
- `README.md` — описание проекта.

---

## 🧩 Что реализовано

### ✔ Загрузка данных (`load_data()`)
- Датасет Wine загружается через `load_wine()`.
- Создаётся DataFrame с признаками и названием класса.
- Целевая переменная отображается как название сорта вина.

### ✔ Анализ целевой переменной (`target_analysis()`)
- Частоты классов.
- Проценты от общего количества.

### ✔ Статистика признаков (`feature_statistics()`)
Для каждого числового признака считаются:
- среднее,
- медиана,
- стандартное отклонение,
- размах (max − min).

### ✔ Визуализация целевой переменной (`visualize_target()`)
- Столбчатая диаграмма.
- Круговая диаграмма.
- Сохранение в PNG.

### ✔ Гистограммы признаков (`visualize_features()`)
- Первые 6 признаков.
- Сетка 3×2.
- Использование `seaborn.histplot`.

### ✔ Boxplot по классам (`features_by_target()`)
- Boxplot для первых 6 признаков.
- Анализ распределения каждого признака внутри классов.

### ✔ Анализ корреляций (`correlation_analysis()`)
- Матрица корреляции.
- heatmap через `seaborn.heatmap`.
- Вывод части корреляционной матрицы в терминал.

---

## ▶ Как запустить проект

Убедитесь, что установлены библиотеки:

```bash
pip install pandas numpy matplotlib seaborn scikit-learn
